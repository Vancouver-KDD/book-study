### CH. 7 - Process of remove duplication

Better `equals` function 

- should include comparing the `class`
    - **`&& getClass().equals(money.getClass());` : will be replaced after `currency` implementation**